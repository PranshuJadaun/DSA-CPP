#include <iostream>
using namespace std;
int main(){
    int a;
    if (3+2%5) //it is a bool if 1 then will run and any no. other than 0 is considered as true which is 1. 
        cout<<"This works";
    if (a=10) //this is assignment  //assignment then putting it as 10 which is true in boolean.
        cout<<"This also works";
    if (-5) //only expression 
        cout<<"Suprisingly This also works";
    
}